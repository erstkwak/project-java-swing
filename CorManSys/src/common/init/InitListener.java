package common.init;

import common.Listener.DeaListener;
import common.Listener.IsoListener;
import common.Listener.OffListener;
import common.Listener.PatListener;
import common.Listener.TreListener;

public class InitListener {

	DeaListener dl = new DeaListener();
	IsoListener il = new IsoListener();
	OffListener ol = new OffListener();
	PatListener pl = new PatListener();
	TreListener tl = new TreListener();
	
}